@page
@model ProyectoProgramacionAvanzadaWeb.Pages.Admin.Proveedor.IndexModel

@{
    Layout = "~/Pages/Shared/_LayoutAdminDashboard.cshtml";
    ViewData["Title"] = "Index";
}

<div class="pagetitle">
    <h1>Lista de Proveedores</h1>
    <nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="index.html">Admin</a></li>
            <li class="breadcrumb-item">Proveedores</li>
            <li class="breadcrumb-item active">Listar</li>
        </ol>
    </nav>
</div>

<section class="section">
    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Proveedores</h5>
                    @if (TempData["SuccessMessage"] != null)
                    {
                        <div class="alert alert-success alert-dismissible fade show" role="alert">
                            <h4 class="alert-heading">Operación exitosa:</h4>
                            <hr>
                            <p>@TempData["SuccessMessage"]</p>
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Cerrar"></button>
                        </div>
                    }
                    <p>
                        <a asp-page="./Create" class="btn btn-success">
                            <i class="bi bi-person-plus"></i> Crear Proveedor
                        </a>
                    </p>
                    @if (!string.IsNullOrEmpty(Model.Message))
                    {
                        <div class="alert alert-danger alert-dismissible fade show" role="alert">
                            <h4 class="alert-heading">Error interno del servidor:</h4>
                            <hr>
                            <p>@Model.Message</p>
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Cerrar"></button>
                        </div>
                    }
                    else
                    {
                        <table id="dataTable" class="table table-striped table-bordered">
                            <thead class="thead-light">
                                <tr>
                                    <th>Id Proveedor</th>
                                    <th>Nombre del Proveedor</th>
                                    <th>Ubicación de la Empresa</th>
                                    <th>Correo Electrónico de la Empresa</th>
                                    <th>Número Telefónico de la Empresa</th>
                                    <th>Acciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model.Proveedores)
                                {
                                    <tr>
                                        <td>@item.IdProveedor</td>
                                        <td>@Html.DisplayFor(modelItem => item.NombreProveedor)</td>
                                        <td>@Html.DisplayFor(modelItem => item.UbicacionEmpresa)</td>
                                        <td>@Html.DisplayFor(modelItem => item.CorreoElectronicoEmpresa)</td>
                                        <td>@Html.DisplayFor(modelItem => item.NumeroTelefonicoEmpresa)</td>
                                        <td>
                                            <div class="btn-group">
                                                <a asp-page="./Edit" asp-route-id="@item.IdProveedor" class="btn btn-primary">
                                                    <i class="bi bi-pencil"></i>
                                                </a>
                                                <a asp-page="./Details" asp-route-id="@item.IdProveedor" class="btn btn-info">
                                                    <i class="bi bi-info-circle"></i>
                                                </a>
                                                <a asp-page="./Delete" asp-route-id="@item.IdProveedor" class="btn btn-danger">
                                                    <i class="bi bi-trash"></i>
                                                </a>
                                            </div>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    }
                </div>
            </div>
        </div>
    </div>
</section>
